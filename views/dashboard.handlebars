<div>
    <h2>This is the dashboard</h2>

    <p>{{#if loggedInOrNot}}
        You Are logged in!!
        {{/if}}
    </p>

    {{#each refinedBlogs as |blogs|}}
    <a href="/blog/{{id}}">
    <div class="blogdiv">
        <h3>{{title}} Created on {{format_date createdAt}}</h3>
        <div class="content-box">
            {{contents}}
        </div>

    </div>
    </a>
    {{/each}}
<a href="/new-post">Click here to enter a new post</a>
</div>